[
  { "id": "g01", "gate": "gate_no_redirect_outside_middleware", "should_fire": true,
    "diff": "--- a/app/login/page.tsx\n+++ b/app/login/page.tsx\n@@ -1,5 +1,6 @@\n import { redirect } from 'next/navigation'\n+redirect('/dashboard')\n export default function LoginPage() {",
    "note": "redirect() in a page file — should fire" },
  { "id": "g02", "gate": "gate_no_redirect_outside_middleware", "should_fire": false,
    "diff": "--- a/middleware.ts\n+++ b/middleware.ts\n@@ -1,4 +1,5 @@\n import { redirect } from 'next/navigation'\n+redirect('/login')\n export function middleware(req) {",
    "note": "redirect() in middleware.ts — should NOT fire" },
  { "id": "g03", "gate": "gate_no_redirect_outside_middleware", "should_fire": true,
    "diff": "--- a/lib/auth.ts\n+++ b/lib/auth.ts\n@@ -3,4 +3,5 @@\n+  redirect('/unauthorized')\n   return null",
    "note": "redirect() in lib/auth.ts — should fire" },
  { "id": "g04", "gate": "gate_no_redirect_outside_middleware", "should_fire": false,
    "diff": "--- a/components/Button.tsx\n+++ b/components/Button.tsx\n@@ -1,4 +1,5 @@\n+const redirectUrl = '/home'\n export const Button = () => <button>{redirectUrl}</button>",
    "note": "redirectUrl variable, not redirect() — should NOT fire" },

  { "id": "g05", "gate": "gate_no_router_import", "should_fire": true,
    "diff": "--- a/components/Nav.tsx\n+++ b/components/Nav.tsx\n@@ -1,4 +1,5 @@\n+import { useRouter } from 'next/router'\n export const Nav = () => {",
    "note": "next/router import — should fire" },
  { "id": "g06", "gate": "gate_no_router_import", "should_fire": false,
    "diff": "--- a/components/Nav.tsx\n+++ b/components/Nav.tsx\n@@ -1,4 +1,5 @@\n+import { useRouter } from 'next/navigation'\n export const Nav = () => {",
    "note": "next/navigation import — should NOT fire" },
  { "id": "g07", "gate": "gate_no_router_import", "should_fire": true,
    "diff": "--- a/hooks/useNav.ts\n+++ b/hooks/useNav.ts\n@@ -1,4 +1,5 @@\n+import Router from 'next/router'\n export const useNav = () => Router.push",
    "note": "default import from next/router — should fire" },
  { "id": "g08", "gate": "gate_no_router_import", "should_fire": false,
    "diff": "--- a/hooks/useNav.ts\n+++ b/hooks/useNav.ts\n@@ -1,4 +1,5 @@\n+import { usePathname } from 'next/navigation'\n export const useNav = () => usePathname()",
    "note": "usePathname from next/navigation — should NOT fire" },

  { "id": "g09", "gate": "gate_no_gSSP", "should_fire": true,
    "diff": "--- a/pages/product.tsx\n+++ b/pages/product.tsx\n@@ -1,4 +1,6 @@\n+export async function getServerSideProps() {\n+  return { props: {} }\n+}\n export default function ProductPage() {",
    "note": "getServerSideProps export — should fire" },
  { "id": "g10", "gate": "gate_no_gSSP", "should_fire": false,
    "diff": "--- a/app/product/page.tsx\n+++ b/app/product/page.tsx\n@@ -1,4 +1,6 @@\n+export default async function ProductPage() {\n+  const data = await fetchData()\n+  return <div>{data}</div>\n+}",
    "note": "async server component (correct pattern) — should NOT fire" },
  { "id": "g11", "gate": "gate_no_gSSP", "should_fire": true,
    "diff": "--- a/pages/index.tsx\n+++ b/pages/index.tsx\n+export const getServerSideProps = async (context) => {\n+  return { props: { data: null } }\n+}",
    "note": "const getServerSideProps = — should fire" },
  { "id": "g12", "gate": "gate_no_gSSP", "should_fire": true,
    "diff": "--- a/lib/data.ts\n+++ b/lib/data.ts\n+// Previously used getServerSideProps, now using fetch\n+export const getProductData = async () => fetch('/api/products')",
    "note": "comment mentions gSSP — gate fires on // comments too (only # is excluded)" },

  { "id": "g13", "gate": "gate_no_ts_ignore", "should_fire": true,
    "diff": "--- a/components/Form.tsx\n+++ b/components/Form.tsx\n+// @ts-ignore\n+const value = event.target.value as any",
    "note": "@ts-ignore comment — should fire" },
  { "id": "g14", "gate": "gate_no_ts_ignore", "should_fire": false,
    "diff": "--- a/components/Form.tsx\n+++ b/components/Form.tsx\n+const value = (event.target as HTMLInputElement).value",
    "note": "proper type assertion — should NOT fire" },
  { "id": "g15", "gate": "gate_no_ts_ignore", "should_fire": true,
    "diff": "--- a/lib/api.ts\n+++ b/lib/api.ts\n+/* @ts-nocheck */\n+export const client = createClient(config)",
    "note": "@ts-nocheck comment — should fire" },
  { "id": "g16", "gate": "gate_no_ts_ignore", "should_fire": false,
    "diff": "--- a/lib/api.ts\n+++ b/lib/api.ts\n+export const client = createClient(config as ClientConfig)",
    "note": "cast without suppress — should NOT fire" },

  { "id": "g17", "gate": "gate_no_console_log", "should_fire": true,
    "diff": "--- a/components/Product.tsx\n+++ b/components/Product.tsx\n+console.log('rendering product', id)",
    "note": "console.log added in component — should fire" },
  { "id": "g18", "gate": "gate_no_console_log", "should_fire": false,
    "diff": "--- a/components/Product.tsx\n+++ b/components/Product.tsx\n+const label = 'product console'\n+return <div>{label}</div>",
    "note": "string contains 'console' but no console.log call — should NOT fire" },
  { "id": "g19", "gate": "gate_no_console_log", "should_fire": true,
    "diff": "--- a/lib/auth.ts\n+++ b/lib/auth.ts\n+  console.log('token', token)",
    "note": "console.log in lib code — should fire" },
  { "id": "g20", "gate": "gate_no_console_log", "should_fire": false,
    "diff": "--- a/lib/auth.ts\n+++ b/lib/auth.ts\n+  logger.info('token acquired')",
    "note": "proper logger call — should NOT fire" },

  { "id": "g21", "gate": "run_all_gates", "should_fire": false,
    "diff": "--- a/components/SaleBadge.tsx\n+++ b/components/SaleBadge.tsx\nnew file mode 100644\n@@ -0,0 +1,8 @@\n+import React from 'react'\n+\n+interface Props { label: string }\n+\n+export const SaleBadge = ({ label }: Props) => (\n+  <span className=\"badge-sale\">{label}</span>\n+)\n+\n+export default SaleBadge",
    "note": "clean new component — all gates should pass" },
  { "id": "g22", "gate": "run_all_gates", "should_fire": false,
    "diff": "--- a/middleware.ts\n+++ b/middleware.ts\n@@ -1,5 +1,8 @@\n import { NextResponse } from 'next/server'\n+import { redirect } from 'next/navigation'\n \n export function middleware(request: Request) {\n+  if (!request.headers.get('auth')) redirect('/login')\n   return NextResponse.next()\n }",
    "note": "redirect in middleware + clean imports — all gates pass" },
  { "id": "g23", "gate": "run_all_gates", "should_fire": true,
    "diff": "--- a/components/Checkout.tsx\n+++ b/components/Checkout.tsx\n@@ -1,5 +1,8 @@\n+import { useRouter } from 'next/router'\n+console.log('checkout loaded')\n+// @ts-ignore\n+redirect('/cart')",
    "note": "all four violations in one diff — run_all_gates should fire on first hit" },
  { "id": "g24", "gate": "run_all_gates", "should_fire": false,
    "diff": "--- a/app/checkout/page.tsx\n+++ b/app/checkout/page.tsx\n@@ -1,6 +1,12 @@\n import { useRouter } from 'next/navigation'\n import type { CheckoutItem } from '@/types'\n \n export default async function CheckoutPage() {\n+  const items: CheckoutItem[] = await getItems()\n+  return <div>{items.map(i => <span key={i.id}>{i.name}</span>)}</div>\n }",
    "note": "clean checkout page with correct imports — all gates pass" }
]
